# AUDIRE 실험 결과

> **RQ1–RQ3와 E11 수치는 모두 합성(synthetic) 데이터에서 산출됐습니다.** 시뮬레이터가
> 만들어 넣은 구조의 복원과 설계 민감도만 보여주며 사람 청취자 근거가 아닙니다. §7의 ASR
> 수치만 고정 공개 Zeroth-Korean 음성의 실제 추론 결과이고, 이 역시 임상적 지위나 청취자별
> 오청 효능을 갖지 않습니다. `docs/RISK_REGISTER.md` S1·S2 참조.

`experiments/registry.yaml`의 실행 기록으로 추적 가능한 지표만 "결과"로 기재합니다.
그 외는 "관찰" 또는 "구조적 논증"으로 명시합니다.

---

## 0. 측정된 것과 측정되지 않은 것

| 질문 | 상태 |
|---|---|
| RQ1 — 개인 혼동행렬이 PTA / PTA+SRT+WRS를 넘어 예측을 개선하는가 | 측정됨 (합성) |
| RQ2 — 동일 자막 비율에서 개인화 순위가 베이스라인을 이기는가 | 측정됨 (합성) |
| RQ3 — 개인화 임계값이 전역 임계값보다 나은가 | 측정됨 (합성) |
| 모델 계열 비교 (선형 vs 비선형 vs 결정론적) | 측정됨 (합성) |
| RQ4 — SNR·화자·음절 위치 강건성 | 부분 측정, 스윕 미실행 |
| 교정 길이 연구 (E5) | **미실행** |
| 개인 특이성 × 교정 길이 스윕 (E11) | 측정됨 (합성, §6) |
| E7 실제 ASR WER/CER·타임스탬프 | 고정 10화자 소규모 회귀 측정됨 (§7); 전체 457개 미실행 |
| G6 API·웹 정상/실패 경로 | 실행 검증됨 (공학 검증; 효능 결과 아님) |
| 전문가 검토 (E9) | **미시행** |
| 사람 청취자 결과 | **없음 — 참여자 데이터가 존재하지 않음** |

---

## 1. 기록된 실행

| 실행 | 설정 파일 | 시드 | 상태 |
|---|---|---|---|
| `smoke-*` | `experiments/configs/smoke.yaml` | 1 | 완료 |
| `rq1_main-20260816T051829Z-0de3701` | `experiments/configs/rq1_main.yaml` | 5 | **완료** |
| `sens_smoke-*` | 민감도 스윕 코드 경로 검증 | 1 | 완료 |
| `sensitivity_idiosyncrasy_calibration-20260816T061412Z-150053c` | `experiments/configs/sensitivity.yaml` | 3 | **완료 (27칸)** |
| `asr_zeroth_10speaker-20260816T065158Z-150053c` | `experiments/configs/asr_eval.yaml` | 해당 없음 | **완료 (실제 추론)** |

두 본 실행 모두 레지스트리에 `git_dirty: true`로 기록됐습니다. 수치와 전체 설정은 아티팩트와
레지스트리에 보존됐지만, 깨끗한 커밋에서의 최종 릴리스 재실행은 아직 필요합니다. 이 이력을
숨기거나 사후에 SHA를 바꾸지 않습니다.

재현 명령:

```bash
make eval
make sensitivity
```

기록된 아티팩트만으로 모든 표와 그림을 재생성:

```bash
make figures
```

산출물은 `experiments/artifacts/<run_id>/` 아래에 생성됩니다:
`arm_metrics.json`, `contrasts.json`, `caption_frontier.json`,
`threshold_comparison.json`, `cohort_summaries.json`, `summary.json`,
`figures/table_*.csv`, `figures/fig_*.png`.

---

## 2. 합성 코호트의 성질

생성기는 Joo et al. (2026, DOI 10.21848/asr.250214)이 보고한 청력군별 단음절 오류율
**18.3 / 27.8 / 48.4 / 80.4 %** — 즉 정반응률 0.817 / 0.722 / 0.516 / 0.196 — 에
정박(anchor)되어 있습니다.

`solve_position_error_mass()`가 이 **음절 단위** 정확도를 각 위치(초성·중성·종성)의
오류 질량으로 변환합니다. 이 변환이 없으면 0.817이 위치마다 적용되어 음절 정확도가
0.55 부근으로 떨어지고, 모든 합성 청취자가 문헌보다 훨씬 나쁜 상태가 됩니다.
왕복 변환은 1e-6 오차까지 테스트로 검증됩니다.

달성된 코호트 정확도는 설정값보다 약간 낮습니다. 청취자 간 분산이 로짓 척도에서
적용되기 때문입니다(옌센 부등식). 이 차이는 보정하지 않고 **그대로 보고**합니다 —
`Cohort.syllable_accuracy_by_stratum()`이 모든 코호트 요약에 설정값과 나란히 실립니다.

**단어 단위 오청 기저율(base rate)은 관찰된 사실이 아니라 설정된 성질입니다.**
`TrialModel`에 가정(assumption)으로 선언된 어휘 복구(lexical repair) 파라미터에서
따라 나옵니다. PR-AUC는 기저율에 의존하므로, 독자가 바닥값을 볼 수 있도록 모든 코호트
요약과 계층별로 함께 보고합니다.

---

## 3. RQ1 — 개인 혼동행렬은 예측 정보를 더하는가?

**답 (합성): 그렇다. 신뢰할 수 있게, 그러나 PTA+SRT+WRS 대비 작은 폭으로.**

### 3.1 모델 계열 비교 (5시드 집계, `rq1_main` 실행 완료)

실행 `rq1_main-20260816T051829Z-0de3701` · 시드 [101, 202, 303, 404, 505] ·
청취자 80명 · 교정 100시행 · 단어 250시행 · 청취자 수준 5-폴드 · 부트스트랩 1000회.
코호트 오청 기저율 0.3559 ± 0.0174.

**아래는 12개 arm 평가 전부이며 선별하지 않았습니다.** 값은 5시드 평균 ± 표준편차입니다.

| 특징 조합 (arm) | 모델 | PR-AUC | ROC-AUC | Brier | ECE | F1 |
|---|---|---|---|---|---|---|
| `clinical_plus_confusion` | 로지스틱 | **0.6685 ± 0.0214** | **0.7766** | **0.1770** | 0.0086 | 0.5711 |
| `clinical` | 로지스틱 | 0.6613 ± 0.0216 | 0.7696 | 0.1793 | 0.0106 | 0.5599 |
| `confusion_only` | 로지스틱 | 0.6571 ± 0.0216 | 0.7649 | 0.1809 | 0.0102 | 0.5578 |
| `pta_only` | 로지스틱 | 0.6337 ± 0.0186 | 0.7430 | 0.1874 | 0.0119 | 0.5288 |
| `word_context_only` | 로지스틱 | 0.4253 ± 0.0189 | 0.5935 | 0.2234 | 0.0174 | 0.1345 |
| `clinical_plus_confusion` | 그래디언트 부스팅 | 0.6617 ± 0.0236 | 0.7720 | 0.1784 | 0.0101 | 0.5750 |
| `clinical` | 그래디언트 부스팅 | 0.6565 ± 0.0241 | 0.7665 | 0.1801 | 0.0159 | 0.5741 |
| `confusion_only` | 그래디언트 부스팅 | 0.6488 ± 0.0232 | 0.7592 | 0.1826 | 0.0108 | 0.5577 |
| `pta_only` | 그래디언트 부스팅 | 0.6194 ± 0.0212 | 0.7293 | 0.1915 | 0.0310 | 0.5304 |
| `word_context_only` | 그래디언트 부스팅 | 0.4205 ± 0.0178 | 0.5889 | 0.2236 | 0.0081 | 0.0927 |
| `confusion_only` | 음소독립 `R_phon` | 0.6435 ± 0.0236 | 0.7493 | **0.4175** | **0.4661** | 0.5414 |
| `clinical_plus_confusion` | 음소독립 `R_phon` | 0.6435 ± 0.0236 | 0.7493 | 0.4175 | 0.4661 | 0.5414 |
| (유병률 바닥값) | — | 0.3559 | 0.5000 | — | — | — |

**핵심 소견 1 — 비선형 모델이 이기지 못했습니다.** 5시드 전체에서 그래디언트 부스팅이
모든 arm에서 로지스틱 회귀와 같거나 낮았습니다(최선 arm 0.6617 대 0.6685).
따라서 최종 모델로 **해석 가능한 로지스틱 회귀를 경험적 근거에 따라 선택**합니다(ADR-0012).
복잡한 모델을 AI처럼 보이게 하려고 채택하지 말라는 연구 계획의 요구가 데이터로 충족되었습니다.

**핵심 소견 2 — 순서는 모든 모델 계열에서 동일합니다.**
`clinical_plus_confusion` > `clinical` > `confusion_only` > `pta_only` > `word_context_only`.
`word_context_only`의 F1이 0.13(로지스틱)·0.09(부스팅)로 붕괴하는 것은, 청취자 정보 없이
단어 특징만으로는 0.5 임계값에서 양성 예측을 거의 하지 못한다는 뜻입니다.

### 3.2 쌍대 대조 (청취자 수준 부트스트랩, 동일 평가 행, 5시드)

`0 제외 시드`는 그 시드의 95 % 신뢰구간이 0을 포함하지 않은 횟수입니다.
**5/5가 아니면 효과가 시드에 따라 일관되지 않았다는 뜻이며, 그대로 보고합니다.**

**로지스틱 회귀 (1차 모델)**

| 대조 | ΔPR-AUC | 0 제외 | ΔBrier | 0 제외 |
|---|---|---|---|---|
| `clinical_plus_confusion` − `clinical` | **+0.0073 ± 0.0017** | **5/5** | −0.0022 ± 0.0006 | 5/5 |
| `clinical_plus_confusion` − `pta_only` | +0.0348 ± 0.0101 | 5/5 | −0.0104 ± 0.0029 | 5/5 |
| `clinical_plus_confusion` − `word_context_only` | +0.2432 ± 0.0073 | 5/5 | −0.0464 ± 0.0038 | 5/5 |
| `confusion_only` − `clinical` | −0.0042 ± 0.0045 | **1/5** | +0.0016 ± 0.0013 | 2/5 |

**그래디언트 부스팅 (비교 모델)**

| 대조 | ΔPR-AUC | 0 제외 | ΔBrier | 0 제외 |
|---|---|---|---|---|
| `clinical_plus_confusion` − `clinical` | +0.0052 ± 0.0018 | **3/5** | −0.0017 ± 0.0004 | 5/5 |
| `clinical_plus_confusion` − `pta_only` | +0.0423 ± 0.0151 | 5/5 | −0.0131 ± 0.0056 | 5/5 |
| `clinical_plus_confusion` − `word_context_only` | +0.2411 ± 0.0138 | 5/5 | −0.0452 ± 0.0045 | 5/5 |
| `confusion_only` − `clinical` | −0.0077 ± 0.0022 | 1/5 | +0.0025 ± 0.0012 | 2/5 |

**해석.**

1. **RQ1 1차 대조는 로지스틱에서 5/5 시드 일관**입니다(+0.0073 PR-AUC). 혼동행렬은 임상
   지표 대비 신뢰할 수 있는 증가분을 주지만 그 크기는 작습니다.
2. **같은 대조가 부스팅에서는 3/5 시드에서만 유의**합니다. 효과가 모델 계열에 따라 일관성이
   다르며, 이는 효과가 작다는 사실과 부합합니다. 로지스틱 결과만 보고했다면 이 사실이
   숨겨졌을 것입니다.
3. **혼동행렬 단독은 임상 지표를 이기지 못합니다** — 두 모델 모두 1/5 시드에서만 유의하고
   방향은 음수입니다. WRS를 대체할 수 없고 보완할 뿐입니다.
4. 순음청력도 단독 대비 이득(+0.035)이 임상 지표 대비 이득(+0.007)의 약 5배입니다.
   **가장 큰 개선은 혼동행렬이 아니라 SRT·WRS를 더하는 데서 옵니다.**

### 3.3 이 결과의 천장에 관한 필수 단서

시뮬레이터에서 WRS는 혼동행렬 대각원소를 결정하는 **바로 그 잠재 능력**의 이항 추정치로
표집됩니다. 따라서 WRS는 위험의 전역 성분에 대해 **구성상 거의 충분통계량**이며,
혼동행렬은 디리클레 섭동이 허용하는 개별 특이 성분만 기여할 수 있습니다.

**이 시뮬레이션은 본 과제의 실제 동기 가설 — 실제 청취자에서 국소 오류 구조가 전역
점수로부터 디리클레 섭동보다 크게 이탈한다 — 를 검정할 수 없습니다.** 그 질문에는
사람 데이터가 필요합니다(차단 요인 B1).

### 3.4 결정론적 베이스라인은 순위는 좋지만 확률이 아니다

`R_phon = 1 − ∏ C_u(φ,φ)` 는 5시드 평균 PR-AUC 0.6435 ± 0.0236 · ROC-AUC 0.7493로
**순위 신호로는 쓸 만하지만**, **Brier 0.4175 · ECE 0.4661** 입니다
(적합된 로지스틱은 0.1770 / 0.0086). Brier가 2.4배, ECE가 54배 나쁩니다.

이것은 유용한 음성(negative) 결과입니다. 음소 독립 곱은 쓸 만한 *순위* 신호이자
심각하게 잘못 보정된 *확률*입니다. **확률 임계값 자막 정책에 직접 투입해서는 안 됩니다.**
등장회귀(isotonic) 보정이 ECE와 Brier를 모두 낮춘다는 사실은 테스트로 강제됩니다.

---

## 4. RQ2 — 동일 자막 비율에서 개인화 순위가 베이스라인을 이기는가?

**답 (합성): 5시드 주 설정에서 개인화 순위는 단어길이를 작게 이겼지만, 이득은 시뮬레이터의
개인 특이성과 교정 길이에 조건부입니다. 보편적 우위로 해석할 수 없습니다.**

오청 단어 재현율의 5시드 평균 ± 표준편차. 시드당 청취자 80명 · 단어 20,000개 ·
기저율 0.3559 ± 0.0174.

### 청취자별 예산 — 실제 배포가 하는 방식

| 전략 | B=10 % | 20 % | 30 % | 50 % | 최하위 청취자 @20 % |
|---|---|---|---|---|---|
| 무작위 | 0.098±0.002 | 0.197±0.002 | 0.298±0.003 | 0.501±0.002 | 0.065±0.039 |
| 단어길이 (비개인화) | 0.132±0.003 | 0.251±0.005 | 0.369±0.006 | 0.578±0.005 | 0.103±0.034 |
| 모델 `word_context_only` | 0.132±0.003 | 0.256±0.004 | 0.372±0.005 | 0.586±0.008 | 0.116±0.024 |
| 모델 `pta_only` | 0.132±0.003 | 0.256±0.005 | 0.372±0.006 | 0.587±0.007 | 0.114±0.025 |
| 모델 `clinical` | 0.133±0.003 | 0.256±0.004 | 0.372±0.006 | 0.587±0.007 | 0.114±0.025 |
| 모델 `confusion_only` | 0.129±0.003 | 0.250±0.005 | 0.365±0.007 | 0.578±0.006 | **0.135±0.040** |
| 모델 `clinical_plus_confusion` | **0.134±0.005** | **0.257±0.004** | 0.371±0.004 | **0.590±0.005** | 0.112±0.037 |

### 통합(pooled) 예산 — 배포가 아니라 메커니즘 대조용

| 전략 | B=10 % | 20 % | 30 % | 50 % | 최하위 청취자 @20 % |
|---|---|---|---|---|---|
| 무작위 | 0.099±0.004 | 0.199±0.003 | 0.299±0.003 | 0.499±0.002 | 0.060±0.039 |
| 단어길이 | 0.132±0.004 | 0.250±0.006 | 0.368±0.006 | 0.577±0.006 | 0.109±0.026 |
| 모델 `word_context_only` | 0.132±0.003 | 0.254±0.005 | 0.369±0.005 | 0.584±0.008 | 0.083±0.030 |
| 모델 `pta_only` | 0.222±0.007 | 0.396±0.008 | 0.526±0.008 | 0.724±0.005 | **0.000** |
| 모델 `clinical` | 0.227±0.006 | 0.408±0.004 | 0.551±0.006 | 0.752±0.013 | **0.000** |
| 모델 `confusion_only` | 0.226±0.006 | 0.406±0.004 | 0.546±0.009 | 0.746±0.013 | **0.000** |
| 모델 `clinical_plus_confusion` | **0.227±0.006** | **0.412±0.004** | **0.556±0.008** | **0.759±0.015** | **0.000** |

**메커니즘.** 두 표가 다른 이유는 모델링 우연이 아니라 구조적 사실입니다.
**청취자 내부에서 상수인 특징은 그 청취자의 단어를 직접 구별할 수 없습니다.** PTA·SRT·WRS는
청취자 수준 상수이므로 청취자별 예산에서 직접적인 순위 신호가 아닙니다. arm 간 작은 차이는
이 특징을 포함해 모델을 다시 적합하면서 단어 특징의 계수가 달라진 결과입니다. 통합 예산에서는
청취자 수준 자체를 순위에 쓸 수 있으므로 모델이 오청이 많은 청취자에게 예산을 몰아줍니다.

그 통합 이득은 공짜가 아닙니다. **최하위 청취자의 재현율을 0.000으로 만듭니다.**
총합 재현율만 보면 이것이 큰 승리로 보고됩니다. `select_budget()`와
`recall_by_listener()`가 두 관점을 모두 강제하며, 통합 전략이 개별 청취자를 굶길 수
있고 청취자별 전략은 그럴 수 없다는 것을 테스트가 검증합니다.

**연구 계획에 대한 함의.** `docs/RESEARCH_PLAN.md`가 지정한 1차 성과지표 —
동일 자막 비율에서의 총합 오청 단어 재현율 — 은 **그 자체만으로는 불충분**합니다.
청취자별 재현율 분포가 공동 1차 지표여야 합니다. 이를 조용히 수정하지 않고 발견으로
기록합니다.

주 설정의 B=20 %에서 `clinical_plus_confusion`은 단어길이보다 평균 +0.006 재현율이 높았고,
다섯 시드 모두 방향이 양수였습니다(+0.0043~+0.0113). 다만 이 비교에는 쌍대 신뢰구간이
없고 절대 이득도 작습니다. **H2는 합성 주 설정에서 방향상 약하게 지지되지만, E11이 보이듯
파라미터 전반에서 보편적으로 지지되지는 않습니다.**

---

## 5. RQ3 — 개인화 임계값 vs 전역 임계값

**답 (합성): 전역 임계값이 총합 재현율에서 이기고 형평성에서 크게 집니다. 총합 비교만으로는
오도됩니다.**

목표 전체 자막 비율 20 % · 시드당 청취자 80명 · 5시드 평균 ± 표준편차:

| 정책 | 달성 비율 | 총합 재현율 | 청취자별 재현율 중앙값 | 최하위 청취자 |
|---|---|---|---|---|
| 단일 전역 τ | 0.2000±0.0000 | **0.4118±0.0038** | 0.0000±0.0000 | 0.0000±0.0000 |
| 청취자별 τ (분위수) | 0.2006±0.0003 | 0.2581±0.0037 | **0.2636±0.0085** | **0.1118±0.0369** |

단일 임계값은 자막 예산 거의 전부를 고위험 청취자에게 씁니다. **청취자의 절반이 자막을
사실상 전혀 받지 못하면서**(청취자별 재현율 중앙값 0.000) 총합 수치는 좋아 보입니다.
청취자별 임계값은 같은 총 자막량으로 모든 청취자를 서비스합니다.

**H3은 원래 서술대로는 지지되지 않습니다** — 개인화 임계값이 총합 오해–자막량 프런티어를
개선하지 않습니다. 형평성 기준에서는 명확히 우수하며, 이는 본 시스템의 접근성 목적에
부합하는 기준입니다. 두 수치를 모두 보고하고 어느 쪽도 숨기지 않습니다.

---

## 6. E11 — 개인 특이성 × 교정 길이 민감도

실행 `sensitivity_idiosyncrasy_calibration-20260816T061412Z-150053c`는 디리클레 집중도
{5, 20, 80} × 교정 길이 {25, 100, 400} × 시드 {101, 202, 303}의 **27칸 전부**를
실행했습니다. 낮은 집중도일수록 청취자별 혼동 구조가 전역 능력에서 더 크게 벗어납니다.

| 집중도 | 교정 시행 | PR-AUC | 오청 재현율 @20 % | 최하위 청취자 | 단어길이 대비 | 이긴 시드 |
|---:|---:|---:|---:|---:|---:|---:|
| 5 | 25 | 0.6972 | 0.2623 | 0.0500 | **+0.0152** | 3/3 |
| 5 | 100 | 0.7079 | 0.2733 | 0.0534 | **+0.0262** | 3/3 |
| 5 | 400 | 0.7246 | 0.2861 | 0.1739 | **+0.0391** | 3/3 |
| 20 | 25 | 0.6749 | 0.2558 | 0.1583 | +0.0013 | 2/3 |
| 20 | 100 | 0.6823 | 0.2584 | 0.1614 | +0.0040 | 3/3 |
| 20 | 400 | 0.6915 | 0.2646 | 0.1576 | +0.0102 | 3/3 |
| 80 | 25 | 0.6769 | 0.2560 | 0.0965 | +0.0041 | 3/3 |
| 80 | 100 | 0.6774 | 0.2553 | 0.1067 | +0.0035 | 2/3 |
| 80 | 400 | 0.6843 | 0.2582 | 0.0863 | +0.0063 | 2/3 |

**결론 (합성).** 개인 특이성이 큰 집중도 5에서는 교정이 길수록 이득이 +0.0152 → +0.0391로
단조 증가하고 모든 시드에서 단어길이를 이깁니다. 집중도 20에서는 25시행이 불안정하지만
100·400시행은 3/3으로 방향이 일치합니다. 집중도 80의 이득은 작고 시드 일관성이 없습니다.
따라서 RQ2 결과는 방법의 보편적 성질도, 단일 파라미터의 우연도 아닙니다. **충분히 개인적인
오류 구조가 존재하고 그것을 충분히 측정한 영역에서만 실질적인 이득이 나타납니다.**

집중도 80·25시행의 3/3과 더 긴 교정의 2/3 역전은 경계 영역의 유한 시드 변동성입니다.
평균 이득 자체가 0.0035~0.0063으로 작으므로 단조 경계를 주장하지 않습니다. 이 수치는
사람 청취자에 대한 효능 근거가 아니라 합성 설계 민감도입니다.

## 7. E7 — 실제 ASR 고정 10화자 회귀

실행 `asr_zeroth_10speaker-20260816T065158Z-150053c`는 Zeroth-Korean test의 10명 화자마다
파일 순서상 첫 발화 1개를 **추론 전에** 고정했습니다(총 101.35초). 모델은
`faster-whisper==1.2.1`, `small` 리비전
`536b0662742c02347bc0e980a01041f333bce120`, CPU int8이며 beam/VAD 설정도 아티팩트에
기록됐습니다.

`ko-basic-v1`은 NFKC·소문자화·문장부호 제거·공백 병합 후 단어를 나눕니다. CER은 한국어
띄어쓰기 차이의 영향을 분리하기 위해 정규화 문자열에서 공백을 제거합니다.

| 항목 | 실제 측정값 |
|---|---:|
| WER | **0.3904** = 57오류 / 146 기준 단어 (대치 34 · 삭제 17 · 삽입 6) |
| 공백 제외 CER | **0.1209** = 52오류 / 430 기준 문자 (대치 46 · 삭제 4 · 삽입 2) |
| 문장 완전 일치 | 0/10 |
| ASR 토큰 신뢰도 제공 | 135/135 |
| 타임스탬프 결함 | **0건 / 10발화** (`Transcript.timing_problems`) |
| 처리 시간 / 오디오 | 24.25초 / 101.35초 |
| 실제시간 계수 RTF | **0.239** |

WER와 공백 제외 CER의 차이는 이 고정 부분집합에서 띄어쓰기/단어 경계가 WER에 주는 영향을
포함합니다. 어느 값도 청취자 `listener_risk`에 입력되지 않으며 ASR 회귀와 오청 예측 평가는
구조적으로 별개입니다.

**범위 제한.** 화자 균형은 있지만 발화가 10개뿐이고 결과도 `git_dirty: true`입니다. 이는
고정 가중치·데이터·정규화가 앞으로 변하지 않았는지 찾는 회귀 기준선입니다. 457개 test
전체의 한국어 ASR 성능 추정, 다른 모델 비교, 사람 청취자 효능 주장이 아닙니다.

## 8. 미해결 질문

1. **Ma et al. (2026)은 중성 16개를 분석**합니다. AUDIRE는 정서법상 21개 전체를 씁니다.
   축소의 근거는 본 과제가 접근 가능한 자료로는 재구성할 수 없습니다. 추측하지 않고
   미해결로 기록합니다(ADR-0004).
2. **Kim et al. (2015)의 신뢰도 천장은 유효**합니다. WRS 검사–재검사 r은 50→25→10단어에서
   0.88 → 0.76 → 0.61로 떨어지고, 10단어에서 95 % 예측구간은 ±26.22점입니다.
   어떤 교정 길이 결과도 짧은 맞춤 검사가 표준화 임상 WRS와 동등하다고 주장할 수 없습니다.

---

## 9. 반증(falsification) 상태

`docs/RESEARCH_PLAN.md`가 정한 반증 조건 대비:

| 조건 | 본 시뮬레이션에서의 상태 |
|---|---|
| 혼동 특징이 임상 베이스라인을 넘어 예측을 개선하지 못함 | **미발동** — 로지스틱 +0.0073 PR-AUC, 5/5 시드 CI가 0 제외 |
| 동일 예산에서 무작위/비개인화를 이기지 못함 | **조건부 미발동** — 주 설정은 단어길이 대비 +0.006, E11은 9칸 중 6칸만 3/3 |
| 확률이 심하게 잘못 보정되고 교정 불가 | 적합 모델은 **미발동**(ECE 0.0086). 원시 `R_phon`은 **발동**(ECE 0.4661), 보정 테스트로 복구됨 |
| 청취자 수준 교차검증에서 개선이 사라짐 | **미발동** — 위 결과 전부가 청취자 수준 out-of-fold |

정직한 요약은 다음과 같습니다: **예측 이득은 합성 데이터에서 작지만 시드 일관성이 있었고,
선택 자막 이득은 개인 특이성과 교정량에 조건부였습니다.** 어느 결과도 사람 청취자 효능을
뜻하지 않으며, 실제 동기 가설을 검정하려면 사람 데이터가 필요합니다.

---

## 10. 모델 학습 및 시스템 검증 지표 요약

| 항목 | 값 |
|---|---|
| 최종 선택 모델 | 로지스틱 회귀 (L2, `C=1.0`) — 경험적 근거로 비선형 모델 대비 우위 |
| 특징 수 | `word_context_only` 14 · `pta_only` 24 · `confusion_only` 30 · `clinical` 34 · `clinical_plus_confusion` 50 |
| 학습 데이터 (시드당) | 청취자 80명 × 단어 250시행 = 20,000행 |
| 분할 방식 | 청취자 수준 층화 5-폴드 (누출 검사 매 폴드 실행) |
| 결측 처리 | 파이프라인 내부 중앙값 대치 + 결측 표시 열 |
| 선택 모델 PR-AUC | 0.6685 ± 0.0214 (유병률 바닥값 0.3559 ± 0.0174) |
| 선택 모델 ROC-AUC | 0.7766 |
| 선택 모델 Brier | 0.1770 |
| 선택 모델 ECE (10구간) | 0.0086 |
| 부트스트랩 | 청취자 재표집 1000회 |
| 자막 감소율 (CRR) @B=20 % | 0.80 |
| 배포 적합 | 5시드 · 독립 합성 청취자 실현 400개 · 단어 시행 100,000개; 홀드아웃 성능 주장 없음 |
| 사용자 경로 | API 정상/실패 계약 + Chromium 프로필→교정→업로드→내보내기 정상/교정 누락 실패 |
| 실제 ASR 회귀 | 10화자 WER 0.3904 · 공백 제외 CER 0.1209 · 타임스탬프 결함 0 · RTF 0.239 |
| 테스트 | 362개 통과 · 3개 opt-in 실제 ASR 테스트 기본 실행에서 skip; 별도 실제 실행 3개 통과 |
| 타입 검사 | `mypy --strict` 60개 파일 무오류 |
| 분기 커버리지 | 전체 84 %; G6 API 90 % · 배포 아티팩트 93 % · E7 평가 100 %; 전체 90 % 목표 미달 |
